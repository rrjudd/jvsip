To run this you need a python environment that includes matplotlib and numpy as 
well as the vsipl modules included with jvsip distribution.

To run at a command line type 

> python bf_ex.py

kw_pyJvsip.ipynb is a version which runs in a notebook environment. 
Start with 
> jupyter notebook
and open up the kw_pyJvsip notebook
Note this is a newer version using pyJvsip as the module instead of vsiputils.
