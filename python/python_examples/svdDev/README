The C VSIP svd is contained in jvsip_svd_f.c and jvsip_svd_d.c in the c_VSIP_src directory.  This 
directory was used for development and contains python code which mirrors the C code.  Other 
sub directories contain other work done during the development process which may be usefull.
